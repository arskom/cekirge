const add_message_txn = require ('./db.js');

function get_message_info (uuid, is_reply, folder, read, localState, mimeID, wdate, files, sender, recipients, headers) {
    
    add_message_txn(uuid, is_reply, )
}